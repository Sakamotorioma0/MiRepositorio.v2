// import es la parte principal devido que debe asegurarse de que react este disponible 
import React from 'react';
// importamos librerias de react
// con react se puede utilizar la sintaxis de JSX (javascript XML)
function App(){

    // utilizamos return para dar salida al codigo html  en ingresar las etiquetas div contenedoras del texto h1
    return (
        <div>
            <h1>primer texto ingresado</h1>
        </div>
    );
}

export default App;
// se envia 